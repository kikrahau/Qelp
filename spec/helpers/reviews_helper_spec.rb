require 'rails_helper'
require 'timecop'

RSpec.describe ReviewsHelper, :type => :helper do
	# context 'time stamp' do
	# 	before do
	# 		@new_time = Time.local(2014)
	# 	end
		
	# 	it "shows the time passed, since the review was posted" do
	# 		Timecop.freeze(Time.local(2014))
	# 		Timecop.travel(1)
	# 		expect(helper.relative_timestamp(Time.local(2014))).to eq '1 second ago'
	# 	end

	# 	it "shows the time passed, since the review was posted" do
	# 		Timecop.freeze(Time.local(2014))
	# 		Timecop.travel(2)
	# 		expect(helper.relative_timestamp(Time.local(2014))).to eq '2 seconds ago'
	# 	end
	# end
end