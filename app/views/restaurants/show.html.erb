<h1><%=@restaurant.name %></h1>
<p><%=@restaurant.description %></p>
<p>Average Rating &nbsp;<%= star_rating(@restaurant.average_rating) %></p>


<% if !@restaurant.reviews.any? %>
	<p>No reviews</p>
<% else %>
	<% @restaurant.reviews.each do |review|%>
	<li class='review'><%= star_rating(review.rating) %> <%= review.content%><i>  - posted by <%= User.find(review.user_id).email %> <%= time_ago_in_words(review.created_at) %> ago</i> <%= link_to 'Endorse', review_endorsements_path(review), class: 'endorsement-link' %> 
		<span class='endorsement-count'> <%= review.endorsements.count %> 
		</span> 
		</li>
	<% end %>
<% end %>

<p><%= link_to 'Leave Review', new_restaurant_review_path(@restaurant) %></p>